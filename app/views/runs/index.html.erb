<h1>Runs Guide</h1>

<ul>
  <% @runs.each do |r| %>
  <li>
    <%= link_to r.name, run_path(r.id) %>
  </li>
  <% end %>
</ul>

<p>
  <button> <%= link_to 'Enter new run', new_run_path %> </button>
</p>
