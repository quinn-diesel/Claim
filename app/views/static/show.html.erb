<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Welcome page</title>

<link href="https://fonts.googleapis.com/css?family=Nova+Flat" rel="stylesheet">


</head>
<body>
  <h1>CLAIM</h1>
  <h2>HOW GOOD IS YOUR CLAIM?</h2>

<source >

  <div class = 'welcome_button'>
    <div id = 'sign-in'>Sign In</div>
    <div id = 'sign-up'>Sign up</div>
    <div id = 'geust'>Enter as geust</div>
    <div id = 'mountain-show'>Check the mountains out</div>
  </div>

  <nav>
  <% unless @current_user.present? %>
    <%= link_to 'Login', login_path %> | <%= link_to 'Signup', new_user_path %>
  <% else %>
    <p>
      Welcome, <%= link_to @current_user.name, user_path(@current_user) %> |
      <%= link_to 'Logout', login_path, method: :delete %>
  </p>
  <% end %>
  </nav>


</body>
</html>
